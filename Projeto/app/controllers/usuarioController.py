from flask import Blueprint, request, render_template, url_for, session, redirect
from models import Usuario
#from authController import Auth


'''usuario_bp = Blueprint('usuario', __name__, template_folder='templates', static_folder='static')

@usuario_bp.route('/')
def home():
    #return render_template('Tela_inicial.html')
    pass


@usuario_bp.route('/perfil', methods=['GET', 'POST'])
def perfil():
    #redirect
    pass
'''

class Participante:

    '''@staticmethod
    def verificaExistenciaOrganizador(senha, email):
        
        if Participante.email == 'senha' or Participante.email=='email':
            return True
        else:
            return False'''
